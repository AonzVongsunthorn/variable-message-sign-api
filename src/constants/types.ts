// #region enum

export enum DatabaseTypes {
  MYSQL = 'mysql',
  MARIADB = 'mariadb',
}

export enum RoleTypes {
  Admin= 'admin',
  Executive = 'executive',
  Manager = 'manager',
  Operator = 'operator',
}

// #endregion enum
